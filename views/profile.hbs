<h1>Profile</h1>

<div class="row">
  <div class="col s6">

    {{#if user.avatarUrl}}
    <img class="avatar" src="{{user.avatarUrl}}" alt="avatar-{{user.name}}">
    {{else}}
    <img src="https://avatar-management--avatars.us-west-2.prod.public.atl-paas.net/default-avatar.png" alt="default">
    {{/if}}
  </div>

  <div class="col s6">
    <form action="/avatar" method="POST" enctype="multipart/form-data">
      <p>Your email: <strong>{{user.email}}</strong></p>
      <h4>Your name: <strong>{{user.name}}</strong></h4>

      <div class="file-field input-field">
        <div class="btn">
          <span>Avatar</span>
          <input type="file" name="avatar">
        </div>
        <div class="file-path-wrapper">
          <input class="file-path validate" type="text">
        </div>
      </div>

      <button type="submit" class="btn">Edit</button>
    </form>
  </div>
</div>

<h4>Send review in our mail</h4>

<form action="/mail" method="post">
  <input type="text" name="message">
  <button type="submit" class="btn">Send</button>
</form>